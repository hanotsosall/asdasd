{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'python bot.py & uvicorn main:app --host 0.0.0.0 --port $PORT'",
    "restartPolicyType": "ON_FAILURE"
  }
}
